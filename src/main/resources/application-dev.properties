# Perfil desarrollo — activar con: -Dspring.profiles.active=dev (o SPRING_PROFILES_ACTIVE=dev)

# JWT — solo para desarrollo local
jwt.secret=cG9jaG9uYW5jZS1kZXYtc2VjcmV0LWtleS0yMDI2LWxvY2FsLXRlc3Rpbmctb25seQ==
jwt.expiration-ms=86400000
spring.datasource.url=jdbc:mysql://localhost:3306/railway?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/Bogota
spring.datasource.username=root
spring.datasource.password=default123

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.springframework.transaction=TRACE
logging.level.org.springframework.orm.jpa=DEBUG

# Credenciales Telegram — bot de PRUEBAS (Pruebas Milton), separado del oficial de producción
app.telegram.bot-token=8790348553:AAG8MB0BI0QvW26QcpIOk-qBsOXcZjOlpoY
app.telegram.chat-id=-1004426378826
app.telegram.group-chat-id=-1004426378826

# API key del bot de conciliacion bancaria (Automatizacion Bancolombia / conciliacion_bancaria.py)
# — separada de la de produccion, para que una prueba local nunca pueda escribir en produccion.
app.conciliacion.api-key=05145732fa3fab8fd053dbcde90988691209b1bbc84a99f3

# Token del bot de Telegram de conciliacion (el mismo TOKEN_TELEGRAM de conciliacion_bancaria.py —
# solo hay UN bot de conciliacion, a diferencia del de retiradores que sí tiene uno de pruebas y
# otro de produccion). Se usa para el auto-trigger al activar una cuenta Bancolombia en Cuentas P2P.
app.conciliacion.bot-token=8968526699:AAG1XzZNFJmosGg3-m9w_Xk8MHSq-j8SlJ4

# ── Bot "Cuentas P2P" — notificaciones de movimientos activados desde Pochonance ──
# Token del bot de Telegram dedicado (creado en BotFather, distinto al de conciliacion
# y al de retiradores — este solo reenvia lo que el sistema de Movimientos detecta).
app.cuentasp2p.bot-token=8807394750:AAFmPgcfmSeLhpL-dWbSE3EEf4qgb9H8vHE
# Chat IDs de confianza que reciben las notificaciones. Por ahora es una lista fija
# manual (separada por comas) — los mismos chats que ya son confiables en el sistema
# de Movimientos (OWNER_CHAT_ID + FULL_NOTIF_IDS en Movimientos/config.py). Cuando se
# quiera un manejo mas fino (por cuenta, con aprobacion, etc.) esto se reemplaza por
# una tabla en base de datos — de momento se agrega/quita gente editando esta lista.
app.cuentasp2p.chats-confiables=1902947261,2135785196
# API key que usa pochonance_bridge.py (Movimientos) para mandar los eventos de
# movimiento a este backend (header X-Bot-Api-Key). Dedicada — no se reutiliza la
# de conciliacion, para que un problema en un sistema no afecte al otro.
app.cuentasp2p.bridge-api-key=9a3d0be9b249132495c8caec17f365d9ac794ce8

# Interruptor de CuentaP2PSyncService (activacion/detencion automatica al
# seleccionar/deseleccionar cuentas P2P). En dev/local va en true por defecto
# para poder probarlo sin afectar produccion (es una base de datos aparte). En
# produccion (application-prod.properties) NO se agrega esta linea a proposito
# — el default en el codigo es false, asi que el backend se puede desplegar
# sin que dispare nada automatico hasta activarlo a mano desde Railway.
app.cuentasp2p.auto-sync-habilitado=true
